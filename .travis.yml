language: csharp
solution: Simple.HttpClientFactory.sln
mono: none
dist: bionic
sudo: required
dotnet: 5.0.100
before_install:
- bash sudo apt-get update
- bash sudo apt-get install -y apt-transport-https
- bash sudo apt-get update
- bash sudo apt-get install -y dotnet-sdk-3.1
- bash ./tools/travis-ci-install-sonar.sh
install:
  - dotnet restore
script:
- bash ./tools/travis-ci-build.sh
