language: csharp
solution: Simple.HttpClientFactory.sln
mono: none
dist: bionic
dotnet: 3.1.201
install:
  - dotnet restore
script:
  - dotnet dev-certs https
  - dotnet build
  - dotnet test
  - dotnet-sonarscanner begin /k:"Simple.HttpClientFactory" /o:"myarichuk" /d:sonar.host.url="https://sonarcloud.io" /d:sonar.login="$(SONAR_TOKEN)"
